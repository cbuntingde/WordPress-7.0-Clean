# Gitleaks config - excludes false positives
# Rules are inferred, we just exclude paths we don't want scanned

[paths]
exclude = [".git", "vendor", "wp-content/plugins", "wp-content/themes", "tests", "docs", "docker"]